// P015A (2026-06-06): the owner-acquisition panel was instance-specific UI and
// was removed from the generic dashboard. Orphaned (no importers), pending
// `git rm`; left empty because the agent sandbox filesystem blocks deletion.
export {};
