// Re-export the canonical contract so consumers can `import { DataConnector } from '@ai-company/connector-framework'`.
export type {
  DataConnector,
  ConnectorHealth,
} from '@ai-company/shared-types';
